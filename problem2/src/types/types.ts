export interface CurencyData {
    currency: string;
    date: string;
    price: number;
}

export interface CurrencyExchangeData {
    send: number,
    receive: number,
    sendAmount: number,
    receiveAmount: number,
}

